name = "solution_ZetaZeroes_v1"
defaultTargets = ["Zeros", "Solution"]

# The core library, for the node's `Conclusions`. Add whatever else the proof needs --
# PNT+, LeanCert, PrimeCert -- here rather than in the core lakefile.
[[require]]
name = "IEANTN"
path = "../.."

[[lean_lib]]
name = "Zeros"
roots = ["Zeros"]

[[lean_lib]]
name = "Solution"
roots = ["Solution"]
